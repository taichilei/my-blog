---
import { defaultLang, BASE } from '../i18n';
const redirectUrl = `${BASE}${defaultLang}/`;
---
<!doctype html>
<html>
  <head>
    <meta http-equiv="refresh" content={`0;url=${redirectUrl}`} />
    <link rel="canonical" href={redirectUrl} />
  </head>
  <body>
    <a href={redirectUrl}>Redirecting...</a>
  </body>
</html>
