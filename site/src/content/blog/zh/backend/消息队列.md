---
Title:"消息队列"

---



# 消息队列

MQ：message queue

- 什么是消息队列？
- 消息队列的三大作用
  - 异步
  - 解耦
  - 削峰

常见的工具🔧

- Rabbit MQ:简单易学、生态活跃适合中小规模应用
- kafka:吞吐量高、延迟极低;适合大数据、日志收集场景
- RocketMQ:阿里出品、支持事务消息适合电商金融等;对可靠性要求高的场景

1. 消息队列的基本概念和主要作用
2. 安装 RabbitMQ，使用管理控制台和SDK 来完成生产者和消费者的消息收发操
3. 学习主流的消息队列模型和对应的适用场景
4. 了解生产环境的实践经验，比如消息可靠性、幂等性、高可用集群、死信队列、延迟队列、优先队列等
5. 深入底层原理，提升问题解決能力和求职面试的竞争力

## Java

spring AMQP

```java
// 生产者
public class TutlSender {
@Autowired
private RabbitTemplate template;
public void send() {
String message = "Hello
World!";
// 发送消息
this. template. convertAndSend( "hello", message);
System.out.println(" [x] Sent
'" + message + "'");
}
}
```

```java
// 创建队列
@Configuration
public class TutiConfig 1
@Bean
	public Queue hello() {
		return new Queue( "hello");
}
```

```java
// 消费者
@RabbitListener (queues = "hello" )
public class TutlReceiver {
@RabbitHandler
public void receive(String in) {
System.out.println(" [x]
Received '" + in + "'");
~
```

